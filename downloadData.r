

# generic function for downloading data files from the internet

downloadData <-  function(){
  
  # download to particular folder
  
  # overwrite warning if downloaded already?
  
  # determine whether file is a single link for a text file of links
  
  # determine whether data is zipped or tar.gz and unpack it 
  
  # run MD5 checksum
  
  return()
}